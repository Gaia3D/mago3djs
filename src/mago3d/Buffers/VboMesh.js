'use strict';

/**
 * @class VboMesh
 * @constructor 
 */
var VboMesh = function () 
{
    if (!(this instanceof VboMesh)) 
	{
		throw new Error(Messages.CONSTRUCT_ERROR);
	}

    // Position, normal, color, texCoord & index.
    // 1- VboKey.
    // vertexAttribPointerParams_array. 1 for position, 1 for normals, etc.

    
};